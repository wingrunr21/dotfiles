require 'consular/iterm'

# You can set specific Consular configurations
# here.
Consular.configure do |c|
  if ENV['PLATFORM'] == 'Darwin'
    c.default_editor = 'mvim'
  else
    c.default_editor = 'vim'
  end
end
